# dependencies
node_modules/

# build output
/web/dist/
/r3
/dist/

# storybook static build
/storybook-static/

# leftover per-repo review state — reviews now live in the global XDG store
# ($XDG_STATE_HOME/r3), so this dir is only stale remnants; still ignored.
.r3/

# nix / direnv
/result
/result-*
.direnv/

# the launcher's README is copied from the root README at publish time
# (npm/package.json `prepack`) — one source of truth, not committed here.
/npm/README.md

/.beads
/CLAUDE.local.md
