# r3 language

## Project grouping

**Project**: An optional group of artifacts with an identity that remains stable
when its repositories move or its display name changes.

**Repository remote**: A network repository identity supplied by a publisher for
project grouping. Several transport spellings or configured aliases can identify
the same project; a remote grants no authority over the artifact.
